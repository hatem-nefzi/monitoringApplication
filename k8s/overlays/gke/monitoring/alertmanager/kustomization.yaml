# k8s/overlays/gke/monitoring/alertmanager/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - configmap.yaml
  - service.yaml

commonLabels:
  app: alertmanager
  component: alerting

namespace: monitoring