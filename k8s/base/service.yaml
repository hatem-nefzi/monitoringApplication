apiVersion: v1
kind: Service
metadata:
  name: monitoring-backend-service
spec:
  selector:
    app: monitoring-backend
  ports:
  - name: http
    port: 80
    targetPort: 9090
    protocol: TCP
  type: ClusterIP